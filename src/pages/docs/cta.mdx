import ElementWrapper from "@/components/Theme/ElementWrapper";
import Simple from "@/components/CTA/Simple";
import RoundedDesign from "@/components/CTA/RoundedDesign";
import SmallCTA from "@/components/CTA/SmallCTA";
import MiniCTA from "@/components/CTA/MiniCTA";
import ConnectionCTA from "@/components/CTA/ConnectionCTA";
import ExploreDataCTA from "@/components/CTA/ExploreDataCTA";
import ConnectionCTASecondary from "@/components/CTA/ConnectionCTASecondary";
import OneLinerCTA from "@/components/CTA/OneLinerCTA";
import DownloadOptionCTA from "@/components/CTA/DownloadOptionCTA";



# CTA Component

A CTA, or Call to Action, is a prompt on a website or piece of content that encourages users to take a specific action, such as clicking a button or signing up for a newsletter.

To integrate these components into your Next.js project, copy and paste the following code:

## Simple CTA

<ElementWrapper
  previewLink="/cta/simple"
  componentPath="CTA/Simple.tsx"
  element={<Simple />}
/>

## Rounded Design

<ElementWrapper
  previewLink="/cta/rounded"
  componentPath="CTA/RoundedDesign.tsx"
  element={<RoundedDesign />}
/>

## Small CTA

The Small CTA section encourages users to sign up for exclusive offers and updates. It includes a user-friendly form and assures users of data security.

<ElementWrapper
  previewLink="/cta/small-cta"
  componentPath="CTA/SmallCTA.tsx"
  element={<SmallCTA />}
/>

## Mini CTA

The Mini CTA section invites users to get full access to the Celebration kit. It features a straightforward form for email submission and a prominent call-to-action button.

<ElementWrapper
  previewLink="/cta/mini-cta"
  componentPath="CTA/MiniCTA.tsx"
  element={<MiniCTA />}
/>

## Connection CTA

The Connection CTA section invites users to join a community of developers. It features a compelling headline, a brief description, and a call-to-action button with an engaging visual element.

<ElementWrapper
  previewLink="/cta/connection-cta"
  componentPath="CTA/ConnectionCTA.tsx"
  element={<ConnectionCTA />}
/>

## Explore Data CTA

The Explore Data CTA section encourages users to gain full access to the Celebration kit, featuring a call-to-action with options to try for free or contact sales. It also provides a link for existing users to log in.

<ElementWrapper
  previewLink="/cta/explore-data"
  componentPath="CTA/ExploreDataCTA.tsx"
  element={<ExploreDataCTA />}
/>

## Connection CTA Secondary

The Connection CTA Secondary component showcases user testimonials and encourages users to join a community of developers using AuraUI. It includes a primary call-to-action button for creating a website and a secondary button for contacting sales.

<ElementWrapper
  previewLink="/cta/connection-cta-secondary"
  componentPath="CTA/ConnectionCTASecondary.tsx"
  element={<ConnectionCTASecondary />}
/>

## One Liner CTA

The One Liner CTA is a compelling call-to-action component that uses a bold gradient background and a striking button to encourage users to get instant access to AuraUI.

<ElementWrapper
  previewLink="/cta/one-liner-cta"
  componentPath="CTA/OneLinerCTA.tsx"
  element={<OneLinerCTA />}
/>

## Download Option CTA

The Download Option CTA is an effective call-to-action component designed to provide users with instant access to AuraUI. It features a gradient background, a clear heading, descriptive text, and download buttons for different platforms.

<ElementWrapper
  previewLink="/cta/download-option-cta"
  componentPath="CTA/DownloadOptionCTA.tsx"
  element={<DownloadOptionCTA />}
/>
