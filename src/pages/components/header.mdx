import ElementWrapper from "@/components/Theme/ElementWrapper";
import Simple from "@/components/Header/Simple";
import Authentication from "@/components/Header/Authentication";
import CallToAction from "@/components/Header/CallToAction";
import UserAccount from "@/components/Header/UserAccount";
import HorizonNav from "@/components/Header/HorizonNav";

# Header Component

## Overview

This Header Elements Section Contains Various Header components.

## Usage

To integrate a Header Components into your Next.js Project,copy and paste the following code.

## Simple Header

This navbar features a straightforward design with minimal items and a clear layout, suitable for basic navigation needs.

<ElementWrapper
  previewLink="/header/cart"
  componentPath="Header/Simple.tsx"
  element={<Simple />}
/>

## User Account Navbar

This navbar includes links for signing up, signing in, and a user profile icon, making it ideal for websites that require user authentication and account management.

<ElementWrapper
  previewLink="/header/with-cta"
  componentPath="Header/UserAccount.tsx"
  element={<UserAccount />}
/>

## Call-to-Action Navbar

This navbar prominently features a call-to-action button ("Get started now") along with standard navigation links, perfect for websites looking to drive user engagement or conversions.

<ElementWrapper
  previewLink="/header/simple"
  componentPath="Header/CallToAction.tsx"
  element={<CallToAction />}
/>

## Authentication Navbar

The Authentication Navbar is designed for websites that require user login and registration features. It includes links for signing up, signing in, and a user profile icon, making it easy for users to manage their accounts seamlessly.

<ElementWrapper
  previewLink="/header/with-shadow"
  componentPath="Header/Authentication.tsx"
  element={<Authentication />}
/>

{/\* ## HorizonNav

HorizonNav is designed with modern web practices in mind, offering a responsive and accessible navigation solution. It features a clean, professional look that aligns perfectly with contemporary design trends.

<ElementWrapper
  previewLink="/header/with-shadow"
  componentPath="Header/HorizonNav.tsx"
  element={<HorizonNav />}
/> */}
