import ElementWrapper from "@/components/Theme/ElementWrapper";
import LoginCard from "@/components/SignIn/LoginCard";

<h1 className="text-gray-200 text-[14px] mt-3 ">
  <span className="text-gray-500 ">Docs ></span> SignIn
</h1>

# SignIn Component

This SignIn Elements Section Contains Various SignIn components.

To integrate a SignIn Components into your Next.js Project,copy and paste the following code.

## SignIn Card Component

The SignIn Card component offers a simple and elegant user interface for users to log into their accounts. It includes input fields for email and password, a "Remember me" checkbox, and options for password recovery and Google sign-in.

<ElementWrapper
  previewLink="/signin/login"
  componentPath="SignIn/LoginCard.tsx"
  element={<LoginCard />}
/>
