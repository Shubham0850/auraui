import ElementWrapper from "@/components/Theme/Element";
import Simple from "@/components/CTA/Simple";
import RoundedDesign from "@/components/CTA/RoundedDesign";
import MobileApp from "@/components/CTA/MobileApp";
import SquareDesign from "@/components/CTA/SquareDesign";
import GetInTouch from "@/components/CTA/GetInTouch";
import MomentumCTA from "@/components/CTA/MomentumCTA";
import ImpactCTA from "@/components/CTA/ImpactCTA";
import CollaborateCTA from "@/components/CTA/CollaborateCTA";
import EngagePlusCTA from "@/components/CTA/EngagePlusCTA";
import JoinUsNowCTA from "@/components/CTA/JoinUsNowCTA";

# CTA Elements

## Overview

A CTA, or Call to Action, is a prompt on a website or piece of content that encourages users to take a specific action, such as clicking a button or signing up for a newsletter.

---

## Usage

To integrate these components into your Next.js project, copy and paste the following code:

## Simple CTA

<ElementWrapper
  previewLink="/cta/simple"
  componentPath="CTA/Simple.tsx"
  element={<Simple />}
/>

## Rounded Design

<ElementWrapper
  previewLink="/cta/rounded"
  componentPath="CTA/RoundedDesign.tsx"
  element={<RoundedDesign />}
/>

{/* 
## MobileApp CTA

<ElementWrapper
  previewLink="/cta/mobileapp"
  componentPath="CTA/MobileApp.tsx"
  element={<MobileApp />}
/>

## GetInTouch CTA

<ElementWrapper
  previewLink="/cta/getintouch"
  componentPath="CTA/GetInTouch.tsx"
  element={<GetInTouch />}
/>

## SquareDesign CTA

<ElementWrapper
  previewLink="/cta/squaredesign"
  componentPath="CTA/SquareDesign.tsx"
  element={<SquareDesign />}
/>

## MomentumCTA

<ElementWrapper
  previewLink="/cta/momentumcta"
  componentPath="CTA/MomentumCTA.tsx"
  element={<MomentumCTA />}
/>

## ImpactCTA

<ElementWrapper
  previewLink="/cta/impactcta"
  componentPath="CTA/ImpactCTA.tsx"
  element={<ImpactCTA />}
/>

## CollaborateCTA

A striking call-to-action section that inspires users to join your collaborative platform. Ideal for highlighting community benefits and encouraging instant access with a sleek design.

<ElementWrapper
  previewLink="/cta/collaboratecta"
  componentPath="CTA/CollaborateCTA.tsx"
  element={<CollaborateCTA />}
/>

## EngagePlusCTA

EngagePlusCTA: A dynamic and responsive call-to-action component that motivates users to take action. Highlight your product’s key features and invite users to try it for free or contact sales with ease.

<ElementWrapper
  previewLink="/cta/engagePluscta"
  componentPath="CTA/EngagePlusCTA.tsx"
  element={<EngagePlusCTA />}
/>

## JoinUsNowCTA

JoinUsNowCTA: A compelling call-to-action component designed for engaging users. Features testimonials, detailed descriptions, and action buttons with React Icons integration for enhanced user interaction."

<ElementWrapper
  previewLink="/cta/join_us_now_cta"
  componentPath="CTA/JoinUsNowCTA.tsx"
  element={<JoinUsNowCTA />}
/> */}
